# Default environment variables for the taskfile
# renamed to .env to avoid being committed to the repository

# Token for GitHub Container Registry (GHCR) authentication
# must have the "read:packages" scope to list images
ghcr_token=
